#!/bin/bash
# Toxic Path Guard: Blocks hardcoded Windows paths
if git diff --cached | grep -E "C:\\|D:\\|E:\\|[a-zA-Z]:\\"; then
  echo "ERROR: Toxic path detected (Windows-style absolute path). Use relative paths or env vars."
  exit 1
fi
